{
  "name": "node",
  "version": "1.0.0",
  "description": "# tutorial global - no window  __dirname - path to current directory require - function to use modules  module = info about current module  process - info about env where the program is being executed module.export = {amount}",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "boostrap": "^2.0.0",
    "lodash": "^4.17.21",
    "nodemon": "^3.1.0"
  }
}
